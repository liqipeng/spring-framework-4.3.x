language: java

jdk:
  - openjdk8

cache:
  directories:
  - '$HOME/.m2/repository'

install:
  - mvn clean package -Dmaven.javadoc.skip=true -B -V

after_success:
  - bash <(curl -s https://codecov.io/bash)

branches:
  only:
    - main

notifications:
  email: false